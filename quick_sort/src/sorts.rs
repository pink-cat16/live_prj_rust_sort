// Sort routines, for i32 vectors.

fn partion(vec: &mut [i32]) -> usize {
    0
}

pub(crate) fn quicksort(vec: &mut [i32]) {
    if vec.len() > 1usize {
        let pivot = partion(vec);
    }
}